<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Light">
        <activity android:name=".ChangePasswordActivity"></activity>
        <activity android:name=".EditProfileActivity2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.main.DriverActivity" />
        <activity android:name=".ui.main.TourGuideActivity" />
        <activity android:name=".ui.main.HotileActivity" />
        <activity android:name=".ui.main.CivilizationActivity" />
        <activity android:name=".ui.NewPasswordActivity" />
        <activity android:name=".ui.ForgetPasswordActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.auth.SignUpActivity" />
        <activity
            android:name=".ui.SplachActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>